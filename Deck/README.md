Thumbnail assets

- `thumbnail.png` is the canonical thumbnail image (590×490).
- `thumbnail.svg` is a simple SVG wrapper that embeds `thumbnail.png` for tools that prefer SVG.

If you want the thumbnail to be used in the app, reference `Deck/thumbnail.png`.
